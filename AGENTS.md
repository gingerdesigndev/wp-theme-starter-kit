# 給接手這個專案的 AI

你正在一個「WP 自刻主題起案包」或它生出來的客戶站裡。這份是入口，先讀完再動手。

> **這份是正本。** `CLAUDE.md` 只是指向這裡的指標，不要在那邊重複內容——兩份一定會漂移。

---

## 這是什麼

用「**古典 PHP 模板 + ACF**」自刻的 WordPress 客戶站主題。

**不使用** Gutenberg、Block Pattern、`theme.json`，也不使用 Elementor 這類頁面建構器。
版面是設計稿切出來的固定結構；可變的只有文案與圖片，那些走 ACF 讓客戶在後台改。

看到 `wp:html`、`register_block_pattern`、`patterns/` 這類東西＝方向錯了，停下來問。

---

## 使用者可能會說什麼，你該做什麼

| 他說 | 你做 |
|---|---|
| 「我要開新站」「把站抓下來改」 | 讀 `docs/00-開站六步.md` |
| 「切這塊」＋ 貼 Figma 連結 | 讀 `docs/01-切版SOP.md` |
| 「讓客戶可以自己改這段字」 | 讀 `docs/02-ACF規範.md` |
| 「改一下樣式」「這裡要窄一點」 | 讀 `docs/03-樣式與設計系統.md` ＋ `design-system.md` |
| 「部署」「傳上去」「上線」 | 讀 `docs/04-部署SOP.md` |
| 「怎麼會這樣」「改了沒反應」 | 讀 `docs/05-踩坑清單.md` ← **先查這份，別自己從頭追** |
| 「收工」「開工」「交接」 | 讀 `docs/06-協作制度.md` |

`.claude/skills/` 裡有對應的 skill，在 Claude Code 底下會自動觸發。
其他工具沒有自動觸發，就照上表自己找路。

### 使用者沒給你 Figma 連結時，這樣引導他

他說「切這塊」卻只描述長相、或貼了截圖，就照這四步教他拿連結，不要憑描述開始切：

1. 在 Figma 點選要切的**那一個 frame／區塊**（看左側圖層面板確認選對層，
   選到整個 page 會讓範圍過大）
2. 按右上角藍色的 **Share**
3. 選 **Copy link to selection**——網址要帶 `?node-id=...`。
   面板裡沒有 Dev Mode 選項的話，先按右上角 `</>` 切到 Dev Mode 再複製
4. 把連結貼過來

**沒有連結就不要開始切。** 憑截圖或口頭描述做出來的版面，尺寸、間距、色票
沒有一項對得上設計稿，返工比重做還久。

如果連結拿到了但你讀不到內容（Figma MCP 沒接上），**直接說你讀不到**，
請他先接好 MCP。不要照著檔名或他的描述猜版面。

---

## 硬規則

1. **不可逆動作先確認。** 部署、啟用主題、覆蓋客戶檔案、對外發佈——
   先列出即將發生什麼，等使用者點頭再做。
2. **版型檔一律走 `st_opt()` 讀 ACF 欄位**，不要直接呼叫 `get_field()`。
   ACF 被停用時 `get_field()` 不存在，直接呼叫會 fatal、整站白畫面。
3. **同一條「要不要顯示」的判斷只能有一份。** 兩個地方各判各的，
   本機（有資料）看起來正常，正式站（沒資料）就壞——而且很難查。
4. **改了 CSS / PHP 就同步更新 `design-system.md`。** 文件與碼是一體的。
5. **改了 PHP 沒反應，先重載 opcache**，不要開始追一個不存在的 bug：
   `kill -USR2 "$(pgrep -f 'php-fpm: master' | head -1)"`
6. **版面問題自己開真實瀏覽器看。** headless 截圖在滿版元素上會給出錯誤結論。
7. **wp-cli 的 socket 會決定你連到哪個站的資料庫，`--path` 擋不住。**
   每個 Local 站的 DB 都叫 `local`、使用者都是 root。寫入前先
   `wp option get siteurl` 確認站對了，不對就停下來。

---

## 你做不到的三件事（別假裝做得到）

這三件只有人能做。遇到就直接說，並告訴使用者怎麼做：

1. **裝 SSH 公鑰到主機**——要輸主機密碼，你不碰密碼。
   請他在自己的電腦上跑 `ssh-copy-id -i ~/.ssh/<專案>.key.pub -p <PORT> <USER>@<HOST>`
2. **產生應用程式密碼**——你沒有後台登入權。
   請他到 WP 後台 → 使用者 → 個人資料 → 應用程式密碼 → 產生，
   然後寫進 repo 外的 `.env`（`600`）。**叫他不要貼進對話。**
3. **到後台啟用主題**——WordPress 核心的 REST API 沒有這個端點。

另外，客戶的 Figma、主機面板、DNS 都在你的視線外，需要時要主動請使用者提供。

---

## 兩條獨立的存取管道（別混淆）

| 管道 | 碰得到 | 憑證 |
|---|---|---|
| SSH | 只有**檔案**（主題部署） | `~/.ssh/<專案>.key` |
| REST API | 只有**內容與設定**（上傳素材、建頁面、改選單、填欄位值） | `.env` 的應用程式密碼 |

兩條互不影響，撤銷其中一條不會動到另一條。

---

## 學到東西要寫下來

- **只對這個客戶有意義**（主機、品牌色、客戶偏好）→ 寫該站的 `MEMORY.md`
- **對下一個站也有用**（踩到的坑、更好的做法）→ 寫 `docs/回饋正本.md`，
  之後由維護者人工回灌起案包正本

每個站起案時的正本版本記在它的 `HANDOFF.md` 開頭，回灌時以那個 commit 為基準。

---

## ⚠️ 起案包本身目前建置中

如果你看到的是**起案包正本**（repo 名 `wp-theme-starter-kit`）而不是某個客戶站，
注意：主題骨架與 `docs/` 底下的八份文件**還在實作中**，上表引用的檔案可能尚未存在。

遇到引用的檔案不存在時，直接告訴使用者那份還沒做，不要自己臨時生一份頂替——
生出來的內容沒有經過驗證，卻會被當成規範。
